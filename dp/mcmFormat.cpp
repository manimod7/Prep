#include <bits/stdc++.h>
using namespace std;
int solve(int arr[], int i, int j){
  int ans;
  //Base Condition
  if(i>j)
    return 0;
  for(int k=i;k<j;k++){
    //tempans = solve(arr,i,k);+ or - solve(arr,k+1,j);
    //ans = func(tempans);
  }
  return ans;
}
int main(){
  
  return 0;
}
